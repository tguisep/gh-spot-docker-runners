# Ansible and ansible-lint both read this. Without it the molecule scenario cannot find the
# role it is testing: molecule sets the path itself at runtime, so the failure only appears
# under the linter, which is a confusing place to meet it.
[defaults]
roles_path = ./roles
